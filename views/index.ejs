<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Voting app</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <script src="https://cdn.socket.io/4.4.1/socket.io.min.js"></script>
  </head>

  <body>
    <!-- Scripts -->
    <script>
      const socket = io("http://localhost:3000");
      socket.on("connection");
    </script>
    <nav>
      <ul>
        <li><a href="/login/">LOGIN</a></li>
        <li><a href="/logout/">LOGOUT</a></li>
      </ul>
    </nav>
    <% let total = 0; candidates.forEach((candidate) => { total +=
    parseInt(candidate.votes, 10); }); %>
    <div id="container">
      <div class="row">
        <div class="col-8 offset-2">
          <h1 class="title text-center my-5">
            Vote For Your Favorite Modern Muscle Car
          </h1>
        </div>
      </div>
      <div class="row">
        <% candidates.forEach((candidate)=> { id = candidate.id;%>
        <form id="photo_container" class="photo_container card col-md-4 p-3">
          <img
            src="<%= candidate.srcUrl %>"
            alt="<%= candidate.altText %>"
            class="photo card-img-top"
          />
          <h4 class="photo_name card-title my-3"><%= candidate.name %></h4>

          <% if (notVoted) { %>
          <a
            class="btn btn-primary vote_btn"
            type="button"
            href="/vote/<%= candidate.id %>"
            >Vote</a
          >
          <% } else { %>
          <h5
            class="votes_results text-center card-subtitle mb-2 text-muted"
            data-car="<%= candidate.id %>"
          >
            <%= candidate.votes %> out of <%= total %> votes
          </h5>
          <script>
            socket.emit(
              "results",
              "<%= candidate.id %>,<%= candidate.votes %> out of <%= total %> votes"
            );
          </script>
          <% } %>
        </form>
        <% }) %>
      </div>
    </div>
    <script>
      socket.on("results", (data) => {
        console.log(data);
        let input = data.split(",");
        document.querySelector("[data-car=" + input[0] + "]").innerHTML =
          input[1];
      });
    </script>
  </body>
</html>

<!-- <!DOCTYPE html>
  <body>

    <div class="container">
      <div class="row">
        <div class="col-8 offset-2">
          <h1>Vote for you favorite car</h1>
        </div>
      </div>
      <div class="row">
        <% candidates.forEach((candidate) => { %>
        <form id="photo-container" class="photo-container card col-md-4 p3">
          <img
            src="<%= candidate.srcUrl %>"
            alt="<%= candidate.altText %>"
            class="photo card-img-top"
          />
          <h4><%= candidate.name %></h4>
          <% if(notVoted) { %>
          <a
            class="btn btn-primary vote-btn"
            type="button"
            href="/vote/<%= candidate.name %>"
            >Vote</a
          >
          <% } else { %>
          <h5
            class="votes-results text-center card-subtitle mb-2 text-muted"
            data-car="<%= candidate.name %>"
          >
            <%= candidate.votes %> out of <%= total %> votes
          </h5>
          <script>
            socket.emit(
              "results",
              "<%= candidate.name %>, <%= candidate.votes %> out of <%= total %> votes"
            );
          </script>
          <% } %>
        </form>
        <% }) %>
      </div>
    </div>

  </body>
</html> -->
